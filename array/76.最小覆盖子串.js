/**
 * @param {string} s
 * @param {string} t
 * @return {string}
 * 1. 用双指针维护一个滑动窗口
 * 2. 移动右指针，找到包含t的子串，移动左指针， 尽量减少包含t的子串的长度
 *  减少的过程中发现不满足了，继续移动右指针，直到移动到最后
 * 3. 循环上述过程，找到最小子串
 */
var minWindow = function (s, t) {}
